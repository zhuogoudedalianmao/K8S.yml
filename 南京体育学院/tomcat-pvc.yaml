kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nanjingtiyu
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nas
  resources:
    requests:
      storage: 100Gi